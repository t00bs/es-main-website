Signature divider — 77×6px solid segment on a full-width 1px 50%-opacity hairline.

```jsx
<SectionRule />
```

Colors come from `--rule-strong` / `--rule-hairline`, so it flips automatically inside `[data-theme="dark"]`.
