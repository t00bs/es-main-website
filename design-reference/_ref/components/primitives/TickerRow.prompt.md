Edge-to-edge row of Neue Pixel labels (20px, +0.075em tracking) used under the hero.

```jsx
<TickerRow items={["Design", "AUTOMATION", "PORTALS"]} />
```

Mixed casing across items is intentional brand texture.
