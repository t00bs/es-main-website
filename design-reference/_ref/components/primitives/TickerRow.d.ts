/**
 * Full-width row of 20px Neue Pixel labels spread edge-to-edge
 * ("Design  AUTOMATION  PORTALS"), used under the hero.
 */
export interface TickerRowProps {
  /** Labels. Mixed casing is brand texture — keep it. */
  items?: string[];
  style?: React.CSSProperties;
}
