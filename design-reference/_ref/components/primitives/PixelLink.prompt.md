Pixel-font link with trailing ⭧ arrow — the brand's only CTA pattern besides outline pills.

```jsx
<PixelLink href="/work/virdaris">View Virdaris Case Study</PixelLink>
<PixelLink color="var(--text-primary)">Meet Toby</PixelLink>
```

Default 18px cream (links usually sit on imagery or dark surfaces). Set `arrow={false}` for a plain pixel label.
