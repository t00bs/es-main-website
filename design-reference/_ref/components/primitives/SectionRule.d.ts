/**
 * The brand's signature divider: a short 77×6px solid segment sitting on a
 * full-width 1px hairline at 50% opacity. Follows the theme (ink / white).
 */
export interface SectionRuleProps {
  style?: React.CSSProperties;
}
