Dark services strip — pixel "We offer:" pinned left with an edge fade, 36px run-on list marquee behind.

```jsx
<ServicesMarquee />
<ServicesMarquee animate={false} text="AI Automations. Custom Portals. …" />
```

Default text is the real service list. Separators alternate periods and bullets — keep that texture.
