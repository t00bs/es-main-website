App-icon / favicon tile — black logomark on cream.

```jsx
<AppIcon size={256} />
<AppIcon size={32} />
```

Geometry matches the "ES Favi" (32px, 4px radius) and "Large ES App" (256px, 16px radius) frames.
